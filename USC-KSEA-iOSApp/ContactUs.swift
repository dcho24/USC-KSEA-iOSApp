import SwiftUI

struct ContactUs: View {
    var body: some View {
        VStack {
            Text("ContactUs")
                .font(.largeTitle)
                .padding()

        }
    }
}

struct ContactUs_Previews: PreviewProvider {
    static var previews: some View {
        ContactUs()
    }
}
